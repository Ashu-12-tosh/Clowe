# Clowe — Nginx reverse proxy
# Replace yourdomain.com everywhere, then see DEPLOYMENT.md for SSL setup.

server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # Let's Encrypt HTTP-01 challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # After SSL is issued, uncomment to force HTTPS:
    # location / { return 301 https://$host$request_uri; }

    # --- Until SSL is set up, serve the app over HTTP directly ---
    client_max_body_size 12m;

    location /api/ {
        proxy_pass http://api:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://api:4000;
        proxy_set_header Host $host;
        expires 7d;
    }

    location / {
        proxy_pass http://web:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- HTTPS server: uncomment after running certbot (see DEPLOYMENT.md) ---
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com www.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#
#     client_max_body_size 12m;
#
#     location /api/ {
#         proxy_pass http://api:4000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /uploads/ {
#         proxy_pass http://api:4000;
#         proxy_set_header Host $host;
#         expires 7d;
#     }
#
#     location / {
#         proxy_pass http://web:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
